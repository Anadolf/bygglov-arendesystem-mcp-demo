{
  "name": "arendesystem-mcp-demo",
  "version": "1.0.0",
  "description": "Demoarendesystem for Bygglov 2.0 with REST API and MCP endpoint.",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/data.js && node --check src/welfareData.js && node --check src/mcp.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  }
}
